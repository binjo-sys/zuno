import React,{useEffect,useMemo,useRef,useState} from 'react';
import * as ReactDOM from 'react-dom/client';
import {Search,Send,Users,Settings,LogOut,Camera,X,MessageCircle,Plus,Menu,ArrowLeft,Phone,Video,Mic,MicOff,VideoOff,PhoneOff} from 'lucide-react';
import './styles.css';
import {api,clearSession,getSession,saveSession} from './lib/api';

function initials(name='User'){return name.split(/\s+/).filter(Boolean).map(x=>x[0]).join('').slice(0,2).toUpperCase()}
function isOnline(user){return !!user?.lastSeen && Date.now()-Number(user.lastSeen)<15000}
function Avatar({user,large=false,showStatus=false,onClick}){const src=user?.avatar||'';return <div onClick={onClick} className={large?'avatar large-avatar avatar-wrap':'avatar avatar-wrap'}>{src?<img src={src} alt="Profile"/>:initials(user?.name)}{showStatus&&<span className={`status-dot ${isOnline(user)?'online':'offline'}`} />}</div>}

const READ_KEY='zuno_read_state';
function loadReadState(){try{return JSON.parse(localStorage.getItem(READ_KEY)||'{}')}catch{return {}}}
function saveReadState(state){localStorage.setItem(READ_KEY,JSON.stringify(state))}

function Auth({onAuth}){
 const [intro,setIntro]=useState(true),[mode,setMode]=useState('login'),[name,setName]=useState(''),[username,setUsername]=useState(''),[about,setAbout]=useState(''),[phone,setPhone]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);
 async function submit(e){e.preventDefault();setBusy(true);setError('');try{const r=mode==='login'?await api.login(phone,password):await api.register(name,phone,password,username.trim(),about.trim());saveSession(r);onAuth(r.user)}catch(e){setError(e.message||'Unable to continue')}finally{setBusy(false)}}
 if(intro)return <div className="auth"><div className="auth-card welcome-card"><div className="logo auth-logo">Z</div><div className="welcome-brand">ZUNO <span>by Binjo</span></div><h1 className="welcome-title">Welcome</h1><p>Connect. Chat. Share.</p><div className="welcome-actions"><button className="primary" onClick={()=>{setMode('register');setIntro(false)}}>Create account</button><button className="secondary" onClick={()=>{setMode('login');setIntro(false)}}>Sign in</button></div><small>Your phone number is your ZUNO account identifier.</small></div></div>;
 return <div className="auth"><div className="auth-card"><button className="back-auth" onClick={()=>{setError('');setIntro(true)}}><ArrowLeft size={17}/> Back</button><div className="logo auth-logo">Z</div><div className="auth-brand">ZUNO <span>by Binjo</span></div><p>{mode==='login'?'Sign in to your account':'Create your ZUNO account'}</p><div className="auth-tabs"><button className={mode==='login'?'on':''} onClick={()=>setMode('login')}>Sign in</button><button className={mode==='register'?'on':''} onClick={()=>setMode('register')}>Create account</button></div><form onSubmit={submit}>{mode==='register'&&<><input required value={name} onChange={e=>setName(e.target.value)} placeholder="Full name"/><input required minLength="3" maxLength="24" pattern="[A-Za-z0-9_]{3,24}" value={username} onChange={e=>setUsername(e.target.value)} placeholder="Username e.g. emmanuel_254"/><textarea maxLength="160" value={about} onChange={e=>setAbout(e.target.value)} placeholder="About you (optional)" rows={3}/></>}<input required type="tel" value={phone} onChange={e=>setPhone(e.target.value)} placeholder="Phone number e.g. 0712 345 678"/><input required minLength="6" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password (6+ characters)"/>{error&&<div className="error">{error}</div>}<button className="primary" disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign in':'Create my ZUNO account'}</button></form><small>Your phone number is your ZUNO account identifier.</small></div></div>
}

function ProfileEditor({me,onClose,onSaved}){
 const [name,setName]=useState(me.name),[username,setUsername]=useState(me.username||''),[about,setAbout]=useState(me.about||''),[photo,setPhoto]=useState(me.avatar||''),[busy,setBusy]=useState(false),[error,setError]=useState(''),input=useRef(null);
 function choose(e){const f=e.target.files?.[0];if(!f)return;if(f.size>1000000){setError('Choose an image under 1MB.');return}const r=new FileReader();r.onload=()=>setPhoto(String(r.result));r.readAsDataURL(f)}
 async function save(){setBusy(true);setError('');try{const u=await api.updateMe({name:name.trim(),username:username.trim(),about:about.trim(),avatar:photo});onSaved(u);onClose()}catch(e){setError(e.message||'Unable to save')}finally{setBusy(false)}}
 return <div className="modal-backdrop"><div className="profile-modal"><button className="modal-close" onClick={onClose}><X/></button><h2>Your profile</h2><div className="profile-large"><Avatar user={{name,avatar:photo,lastSeen:me.lastSeen}} large showStatus/><button className="photo-button" onClick={()=>input.current?.click()}><Camera size={16}/> Choose photo</button><input ref={input} hidden type="file" accept="image/png,image/jpeg,image/webp" onChange={choose}/></div><label>Full name<input value={name} onChange={e=>setName(e.target.value)}/></label><label>Username<input value={username} onChange={e=>setUsername(e.target.value)} placeholder="e.g. emmanuel_254"/></label><label>About<textarea value={about} onChange={e=>setAbout(e.target.value.slice(0,160))} placeholder="Tell people a little about yourself" rows={4}/></label><label>Phone number<input value={me.phone} readOnly/></label>{error&&<div className="error">{error}</div>}<div className="profile-actions"><button className="primary" disabled={busy} onClick={save}>{busy?'Saving…':'Save changes'}</button></div></div></div>
}

function ProfileViewer({user,onClose,onChat}){return <div className="modal-backdrop"><div className="profile-viewer"><button className="modal-close" onClick={onClose}><X/></button><div className="profile-viewer-avatar"><Avatar user={user} large showStatus/></div><h2>{user.name}</h2><div className="profile-username">{user.username?`@${user.username}`:'Username not set'}</div><div className="profile-status">{isOnline(user)?'Online':'Offline'}</div><div className="profile-about"><span>About</span><p>{user.about||'No bio added yet.'}</p></div><div className="profile-viewer-actions"><button className="primary" onClick={onChat}>Message</button></div></div></div>}

function CallOverlay({call,onAccept,onDecline,onHangup,onToggleMute,onToggleVideo,localVideoRef,remoteVideoRef}){
 const isIncoming=call.status==='incoming';
 const isVideo=call.kind==='video';
 return <div className="call-backdrop"><div className={`call-panel ${isVideo?'video-call':''}`}>
   {isVideo?<><video ref={remoteVideoRef} className="remote-video" autoPlay playsInline/><video ref={localVideoRef} className="local-video" autoPlay muted playsInline/></>:<div className="voice-call-avatar"><Avatar user={call.user} large/></div>}
   <div className="call-top"><b>{call.user?.name||'ZUNO user'}</b><span>{isIncoming?'Incoming':call.status==='calling'?'Calling…':'Connected'}</span></div>
   {isIncoming?<div className="call-actions incoming-actions"><button className="call-danger" onClick={onDecline}><PhoneOff size={22}/></button><button className="call-accept" onClick={onAccept}>{isVideo?<Video size={22}/>:<Phone size={22}/>}</button></div>:<div className="call-actions"><button className={`call-control ${call.muted?'active':''}`} onClick={onToggleMute}>{call.muted?<MicOff size={20}/>:<Mic size={20}/>}</button>{isVideo&&<button className={`call-control ${call.videoOff?'active':''}`} onClick={onToggleVideo}>{call.videoOff?<VideoOff size={20}/>:<Video size={20}/>}</button>}<button className="call-danger" onClick={onHangup}><PhoneOff size={22}/></button></div>}
 </div></div>
}

function App(){
 const session=getSession();
 const [me,setMe]=useState(session?.user||null),[people,setPeople]=useState([]),[active,setActive]=useState(null),[viewingProfile,setViewingProfile]=useState(null),[messages,setMessages]=useState([]),[text,setText]=useState(''),[search,setSearch]=useState(''),[profileOpen,setProfileOpen]=useState(false),[mobile,setMobile]=useState(false),[error,setError]=useState(''),[networkOnline,setNetworkOnline]=useState(()=>navigator.onLine),[unread,setUnread]=useState(()=>loadReadState()),[call,setCall]=useState(null);
 const callWsRef=useRef(null),pcRef=useRef(null),localStreamRef=useRef(null),pendingCandidatesRef=useRef([]),localVideoRef=useRef(null),remoteVideoRef=useRef(null);
 const refreshPeople=async()=>{try{const list=await api.users();setPeople(list);if(active){const fresh=list.find(p=>p.id===active.id);if(fresh)setActive(fresh)}if(viewingProfile){const fresh=list.find(p=>p.id===viewingProfile.id);if(fresh)setViewingProfile(fresh)}}catch(e){setError(e.message)}};
 useEffect(()=>{if(!me)return;const goOnline=()=>setNetworkOnline(true),goOffline=()=>setNetworkOnline(false);window.addEventListener('online',goOnline);window.addEventListener('offline',goOffline);api.presence().catch(()=>{});refreshPeople();const heartbeat=setInterval(()=>{if(navigator.onLine){api.presence().catch(()=>{});refreshPeople()}},5000);return()=>{clearInterval(heartbeat);window.removeEventListener('online',goOnline);window.removeEventListener('offline',goOffline)}},[me]);
 useEffect(()=>{if(!me)return;const cleanup=api.connectCalls({onSignal:handleCallSignal});callWsRef.current={};return cleanup},[me]);
 useEffect(()=>{if(!me||!active)return;let alive=true;const load=async()=>{try{const rows=await api.messages(active.id);if(alive){setMessages(rows);const latest=rows.filter(m=>m.sender_id===active.id).reduce((v,m)=>Math.max(v,Number(m.created_at)||0),0);if(latest){const state=loadReadState();state[active.id]=latest;delete state[`count:${active.id}`];saveReadState(state);setUnread(state)}}}catch(e){if(alive)setError(e.message)}};load();const id=setInterval(load,2000);return()=>{alive=false;clearInterval(id)}},[me,active]);
 useEffect(()=>{if(!me||people.length===0)return;let cancelled=false;const checkUnread=async()=>{const state=loadReadState();let changed=false;const currentId=active?.id||'';for(const p of people){if(cancelled||p.id===currentId)continue;try{const rows=await api.messages(p.id);const readAt=Number(state[p.id]||0);const count=rows.filter(m=>m.sender_id===p.id&&Number(m.created_at)>readAt).length;const key=`count:${p.id}`;if(count>0){if(Number(state[key]||0)!==count){state[key]=count;changed=true}}else if(state[key]){delete state[key];changed=true}}catch{}}if(!cancelled&&changed){saveReadState(state);setUnread({...state})}};checkUnread();const timer=setInterval(checkUnread,5000);return()=>{cancelled=true;clearInterval(timer)}},[me,people,active?.id]);
 useEffect(()=>{if(!call)return;const timer=setTimeout(()=>{if(call.status==='calling')endCall(false)},30000);return()=>clearTimeout(timer)},[call]);
 const filtered=useMemo(()=>people.filter(p=>p.name?.toLowerCase().includes(search.toLowerCase())||p.phone?.includes(search)||p.username?.toLowerCase().includes(search.toLowerCase())),[people,search]);
 const shownOnline=user=>user?.id===me?.id?networkOnline:isOnline(user);
 async function openChat(p){setActive(p);setViewingProfile(null);setMobile(false);const state=loadReadState();state[p.id]=Date.now();delete state[`count:${p.id}`];saveReadState(state);setUnread(state);try{setMessages(await api.messages(p.id))}catch(e){setError(e.message)}}
 async function send(){const body=text.trim();if(!body||!active)return;try{const m=await api.sendMessage(active.id,body);setMessages(v=>v.some(x=>x.id===m.id)?v:[...v,m]);const state=loadReadState();state[active.id]=Number(m.createdAt)||Date.now();delete state[`count:${active.id}`];saveReadState(state);setUnread(state);setText('')}catch(e){setError(e.message)}}
 async function signOut(){try{await api.logout()}catch{}clearSession();setMe(null);setActive(null);setMessages([])}
 async function createPeer(kind,otherUserId,isCaller,offer){
   const pc=new RTCPeerConnection({iceServers:[{urls:'stun:stun.l.google.com:19302'}]});
   pcRef.current=pc;
   pc.onicecandidate=e=>{if(e.candidate)api.sendCallSignal(otherUserId,{type:'ice-candidate',candidate:e.candidate});};
   pc.ontrack=e=>{if(remoteVideoRef.current)remoteVideoRef.current.srcObject=e.streams[0];};
   pc.onconnectionstatechange=()=>{if(['failed','disconnected','closed'].includes(pc.connectionState)&&call)endCall(true);};
   const constraints=kind==='video'?{audio:true,video:true}:{audio:true,video:false};
   const stream=await navigator.mediaDevices.getUserMedia(constraints);
   localStreamRef.current=stream;
   if(localVideoRef.current)localVideoRef.current.srcObject=stream;
   stream.getTracks().forEach(track=>pc.addTrack(track,stream));
   if(isCaller){const created=await pc.createOffer();await pc.setLocalDescription(created);await api.sendCallSignal(otherUserId,{type:'offer',kind,offer:created});}
   else if(offer){await pc.setRemoteDescription(new RTCSessionDescription(offer));for(const c of pendingCandidatesRef.current)await pc.addIceCandidate(c).catch(()=>{});pendingCandidatesRef.current=[];const answer=await pc.createAnswer();await pc.setLocalDescription(answer);await api.sendCallSignal(otherUserId,{type:'answer',answer});}
   return pc;
 }
 async function startCall(kind){if(!active||call)return;try{await createPeer(kind,active.id,true);setCall({status:'calling',kind,user:active,muted:false,videoOff:false})}catch(e){setError(e.name==='NotAllowedError'?'Microphone/camera permission was denied.':e.message||'Unable to start call');pcRef.current?.close();pcRef.current=null}}
 async function handleCallSignal(data){const from=people.find(p=>p.id===data.fromUserId)||{id:data.fromUserId,name:'ZUNO user'};const s=data.signal||{};if(s.type==='offer'){if(call)return;setCall({status:'incoming',kind:s.kind||'audio',user:from,fromUserId:data.fromUserId,offer:s.offer,muted:false,videoOff:false});return}
   if(!call||call.user?.id!==data.fromUserId)return;
   if(s.type==='answer'&&pcRef.current){await pcRef.current.setRemoteDescription(new RTCSessionDescription(s.answer));for(const c of pendingCandidatesRef.current)await pcRef.current.addIceCandidate(c).catch(()=>{});pendingCandidatesRef.current=[];setCall(c=>({...c,status:'connected'}));}
   if(s.type==='ice-candidate'&&pcRef.current){if(pcRef.current.remoteDescription)await pcRef.current.addIceCandidate(s.candidate).catch(()=>{});else pendingCandidatesRef.current.push(s.candidate);}
   if(s.type==='hangup'){cleanupCall();setCall(null);}
 }
 async function acceptCall(){if(!call?.fromUserId||call.status!=='incoming')return;try{await createPeer(call.kind,call.fromUserId,false,call.offer);setCall(c=>({...c,status:'connected'}));}catch(e){setError(e.name==='NotAllowedError'?'Microphone/camera permission was denied.':e.message||'Unable to accept call');await api.sendCallSignal(call.fromUserId,{type:'hangup'});cleanupCall();setCall(null)}}
 async function declineCall(){if(call?.fromUserId)await api.sendCallSignal(call.fromUserId,{type:'hangup'});cleanupCall();setCall(null)}
 async function endCall(notify=true){if(!call)return;if(notify&&call.user?.id)await api.sendCallSignal(call.user.id,{type:'hangup'});cleanupCall();setCall(null)}
 function cleanupCall(){localStreamRef.current?.getTracks().forEach(t=>t.stop());localStreamRef.current=null;pcRef.current?.close();pcRef.current=null;pendingCandidatesRef.current=[];if(localVideoRef.current)localVideoRef.current.srcObject=null;if(remoteVideoRef.current)remoteVideoRef.current.srcObject=null}
 function toggleMute(){const track=localStreamRef.current?.getAudioTracks?.()[0];if(!track)return;track.enabled=!track.enabled;setCall(c=>({...c,muted:!track.enabled}))}
 function toggleVideo(){const track=localStreamRef.current?.getVideoTracks?.()[0];if(!track)return;track.enabled=!track.enabled;setCall(c=>({...c,videoOff:!track.enabled}))}
 if(!me)return <Auth onAuth={u=>{setMe(u);saveSession({...getSession(),user:u});api.presence().catch(()=>{});api.users().then(setPeople).catch(()=>{})}}/>;
 return <><div className="app"><aside className={`sidebar ${mobile?'mobile-open':''}`}><div className="brand"><div className="logo">Z</div><span>ZUNO</span><button className="close" onClick={()=>setMobile(false)}><X size={20}/></button></div><button className="profile" onClick={()=>setProfileOpen(true)}><Avatar user={me} showStatus/><div><b>{me.name}</b><small>{me.username?`@${me.username}`:me.phone}</small></div></button><div className="tabs"><button className="active"><MessageCircle size={18}/> Chats</button><button><Users size={18}/> Groups</button></div><div className="search"><Search size={17}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search people"/></div><div className="chat-list">{filtered.map(p=><button className={`chat-row ${active?.id===p.id?'selected':''}`} key={p.id} onClick={()=>openChat(p)}><Avatar user={p} showStatus onClick={e=>{e.stopPropagation();setViewingProfile(p)}}/><div className="chat-info"><div><b>{p.name}</b>{Number(unread[`count:${p.id}`]||0)>0&&<span className="unread-badge">{Math.min(99,Number(unread[`count:${p.id}`]))}</span>}</div><p>{p.username?`@${p.username} · `:''}{shownOnline(p)?'Online':'Offline'}</p></div></button>)}{filtered.length===0&&<div className="empty-chat">No other ZUNO users yet.</div>}</div><button className="new-chat" onClick={()=>filtered[0]&&openChat(filtered[0])}><Plus size={18}/> New conversation</button><div className="side-bottom"><button onClick={()=>setProfileOpen(true)}><Settings size={19}/> Profile & settings</button><button onClick={signOut}><LogOut size={19}/> Sign out</button></div></aside><main className="chat">{active?<><header className="chat-head"><button className="hamb" onClick={()=>setMobile(true)}><Menu/></button><button className="profile-click profile-head-click" onClick={()=>setViewingProfile(active)}><Avatar user={active} showStatus/></button><div className="head-info"><b>{active.name}</b><span className={shownOnline(active)?'online':''}>{shownOnline(active)?'Online':'Offline'}</span></div><div className="call-buttons"><button title="Voice call" onClick={()=>startCall('audio')}><Phone size={18}/></button><button title="Video call" onClick={()=>startCall('video')}><Video size={18}/></button></div></header><section className="messages"><div className="day">TODAY</div>{messages.map(m=><div key={m.id} className={`bubble-wrap ${m.sender_id===me.id?'mine':''}`}><div className="bubble">{m.body}</div></div>)}{messages.length===0&&<div className="empty-chat">No messages yet. Say hello 👋</div>}</section><div className="composer"><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Write a message..."/><button className="send" onClick={send}><Send size={18}/></button></div></>:<div className="empty-state"><div className="logo">Z</div><h2>Welcome to ZUNO</h2><p>Select someone to start chatting.</p></div>}{error&&<div className="error toast">{error}<button onClick={()=>setError('')}><X size={14}/></button></div>}</main></div>{profileOpen&&<ProfileEditor me={me} onClose={()=>setProfileOpen(false)} onSaved={u=>{setMe(u);saveSession({...getSession(),user:u});refreshPeople()}}/>}{viewingProfile&&<ProfileViewer user={viewingProfile} onClose={()=>setViewingProfile(null)} onChat={()=>openChat(viewingProfile)}/>} {call&&<CallOverlay call={call} onAccept={acceptCall} onDecline={declineCall} onHangup={()=>endCall(true)} onToggleMute={toggleMute} onToggleVideo={toggleVideo} localVideoRef={localVideoRef} remoteVideoRef={remoteVideoRef}/>}</>
}

ReactDOM.createRoot(document.getElementById('root')).render(<App/>);
