import React,{useEffect,useMemo,useRef,useState} from 'react';
import * as ReactDOM from 'react-dom/client';
import {Search,Send,Users,Settings,LogOut,Camera,X,MessageCircle,Plus,Menu,ArrowLeft} from 'lucide-react';
import './styles.css';
import {api,clearSession,getSession,saveSession} from './lib/api';

function initials(name='User'){return name.split(/\s+/).filter(Boolean).map(x=>x[0]).join('').slice(0,2).toUpperCase()}
function isOnline(user){return !!user?.lastSeen && Date.now()-Number(user.lastSeen)<15000}
function Avatar({user,large=false,showStatus=false}){const src=user?.avatar||'';return <div className={large?'avatar large-avatar avatar-wrap':'avatar avatar-wrap'}>{src?<img src={src} alt="Profile"/>:initials(user?.name)}{showStatus&&<span className={`status-dot ${isOnline(user)?'online':'offline'}`} />}</div>}

const READ_KEY='zuno_read_state';
function loadReadState(){try{return JSON.parse(localStorage.getItem(READ_KEY)||'{}')}catch{return {}}}
function saveReadState(state){localStorage.setItem(READ_KEY,JSON.stringify(state))}

function Auth({onAuth}){
 const [intro,setIntro]=useState(true),[mode,setMode]=useState('login'),[name,setName]=useState(''),[phone,setPhone]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);
 async function submit(e){e.preventDefault();setBusy(true);setError('');try{const r=mode==='login'?await api.login(phone,password):await api.register(name,phone,password);saveSession(r);onAuth(r.user)}catch(e){setError(e.message||'Unable to continue')}finally{setBusy(false)}}
 if(intro)return <div className="auth"><div className="auth-card welcome-card"><div className="logo auth-logo">Z</div><div className="welcome-brand">ZUNO <span>by Binjo</span></div><h1 className="welcome-title">Welcome</h1><p>Connect. Chat. Share.</p><div className="welcome-actions"><button className="primary" onClick={()=>{setMode('register');setIntro(false)}}>Create account</button><button className="secondary" onClick={()=>{setMode('login');setIntro(false)}}>Sign in</button></div><small>Your phone number is your ZUNO account identifier.</small></div></div>;
 return <div className="auth"><div className="auth-card"><button className="back-auth" onClick={()=>{setError('');setIntro(true)}}><ArrowLeft size={17}/> Back</button><div className="logo auth-logo">Z</div><div className="auth-brand">ZUNO <span>by Binjo</span></div><p>{mode==='login'?'Sign in to your account':'Create your ZUNO account'}</p><div className="auth-tabs"><button className={mode==='login'?'on':''} onClick={()=>setMode('login')}>Sign in</button><button className={mode==='register'?'on':''} onClick={()=>setMode('register')}>Create account</button></div><form onSubmit={submit}>{mode==='register'&&<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Full name"/>}<input required type="tel" value={phone} onChange={e=>setPhone(e.target.value)} placeholder="Phone number e.g. 0712 345 678"/><input required minLength="6" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password (6+ characters)"/>{error&&<div className="error">{error}</div>}<button className="primary" disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign in':'Create my ZUNO account'}</button></form><small>Your phone number is your ZUNO account identifier.</small></div></div>
}

function ProfileEditor({me,onClose,onSaved}){
 const [name,setName]=useState(me.name),[photo,setPhoto]=useState(me.avatar||''),[busy,setBusy]=useState(false),[error,setError]=useState(''),input=useRef(null);
 function choose(e){const f=e.target.files?.[0];if(!f)return;if(f.size>1000000){setError('Choose an image under 1MB.');return}const r=new FileReader();r.onload=()=>setPhoto(String(r.result));r.readAsDataURL(f)}
 async function save(){setBusy(true);setError('');try{const u=await api.updateMe({name:name.trim(),avatar:photo});onSaved(u);onClose()}catch(e){setError(e.message||'Unable to save')}finally{setBusy(false)}}
 return <div className="modal-backdrop"><div className="profile-modal"><button className="modal-close" onClick={onClose}><X/></button><h2>Your profile</h2><div className="profile-large"><Avatar user={{name,avatar:photo,lastSeen:me.lastSeen}} large showStatus/><button className="photo-button" onClick={()=>input.current?.click()}><Camera size={16}/> Choose photo</button><input ref={input} hidden type="file" accept="image/png,image/jpeg,image/webp" onChange={choose}/></div><label>Full name<input value={name} onChange={e=>setName(e.target.value)}/></label><label>Phone number<input value={me.phone} readOnly/></label>{error&&<div className="error">{error}</div>}<div className="profile-actions"><button className="primary" disabled={busy} onClick={save}>{busy?'Saving…':'Save changes'}</button></div></div></div>
}

function ProfileViewer({user,onClose}){
 const online=isOnline(user);
 return <div className="modal-backdrop" onClick={onClose}><div className="profile-modal view-profile" onClick={e=>e.stopPropagation()}><button className="modal-close" onClick={onClose}><X/></button><div className="view-profile-avatar"><Avatar user={user} large showStatus/></div><h2 className="view-profile-name">{user.name}</h2><div className={`view-profile-status ${online?'online':''}`}><span className="status-dot-inline"></span>{online?'Online':'Offline'}</div><div className="view-profile-details"><div><span className="view-label">Phone</span><strong>{user.phone}</strong></div><div><span className="view-label">Username</span><strong>@{(user.username||user.name||'user').toLowerCase().replace(/[^a-z0-9]+/g,'').slice(0,24)}</strong></div><div><span className="view-label">About</span><p>{user.about||'Hey, I\'m on ZUNO.'}</p></div></div><button className="secondary view-close" onClick={onClose}>Close</button></div></div>
}

function App(){
 const session=getSession();
 const [me,setMe]=useState(session?.user||null),[people,setPeople]=useState([]),[active,setActive]=useState(null),[messages,setMessages]=useState([]),[text,setText]=useState(''),[search,setSearch]=useState(''),[profileOpen,setProfileOpen]=useState(false),[viewProfile,setViewProfile]=useState(null),[mobile,setMobile]=useState(false),[error,setError]=useState(''),[networkOnline,setNetworkOnline]=useState(()=>navigator.onLine),[unread,setUnread]=useState(()=>loadReadState());
 const refreshPeople=async()=>{try{const list=await api.users();setPeople(list);if(active){const fresh=list.find(p=>p.id===active.id);if(fresh)setActive(fresh)}}catch(e){setError(e.message)}};
 useEffect(()=>{if(!me)return;const goOnline=()=>setNetworkOnline(true),goOffline=()=>setNetworkOnline(false);window.addEventListener('online',goOnline);window.addEventListener('offline',goOffline);api.presence().catch(()=>{});refreshPeople();const heartbeat=setInterval(()=>{if(navigator.onLine){api.presence().catch(()=>{});refreshPeople()}},5000);return()=>{clearInterval(heartbeat);window.removeEventListener('online',goOnline);window.removeEventListener('offline',goOffline)}},[me]);
 useEffect(()=>{if(!me||!active)return;let alive=true;const load=async()=>{try{const rows=await api.messages(active.id);if(alive){setMessages(rows);const latest=rows.filter(m=>m.sender_id===active.id).reduce((v,m)=>Math.max(v,Number(m.created_at)||0),0);if(latest){const state=loadReadState();state[active.id]=latest;delete state[`count:${active.id}`];saveReadState(state);setUnread(state)}}}catch(e){if(alive)setError(e.message)}};load();const id=setInterval(load,2000);return()=>{alive=false;clearInterval(id)}},[me,active]);
 useEffect(()=>{if(!me||people.length===0)return;let cancelled=false;const checkUnread=async()=>{const state=loadReadState();let changed=false;const currentId=active?.id||'';for(const p of people){if(cancelled||p.id===currentId)continue;try{const rows=await api.messages(p.id);const readAt=Number(state[p.id]||0);const count=rows.filter(m=>m.sender_id===p.id&&Number(m.created_at)>readAt).length;const key=`count:${p.id}`;if(count>0){if(Number(state[key]||0)!==count){state[key]=count;changed=true}}else if(state[key]){delete state[key];changed=true}}catch{}}if(!cancelled&&changed){saveReadState(state);setUnread({...state})}};checkUnread();const timer=setInterval(checkUnread,5000);return()=>{cancelled=true;clearInterval(timer)}},[me,people,active?.id]);
 const filtered=useMemo(()=>people.filter(p=>p.name?.toLowerCase().includes(search.toLowerCase())||p.phone?.includes(search)),[people,search]);
 const selfOnline=networkOnline;
 const shownOnline=user=>user?.id===me?.id?selfOnline:isOnline(user);
 async function openChat(p){setActive(p);setMobile(false);const state=loadReadState();state[p.id]=Date.now();delete state[`count:${p.id}`];saveReadState(state);setUnread(state);try{setMessages(await api.messages(p.id))}catch(e){setError(e.message)}}
 async function send(){const body=text.trim();if(!body||!active)return;try{const m=await api.sendMessage(active.id,body);setMessages(v=>v.some(x=>x.id===m.id)?v:[...v,m]);const state=loadReadState();state[active.id]=Number(m.createdAt)||Date.now();delete state[`count:${active.id}`];saveReadState(state);setUnread(state);setText('')}catch(e){setError(e.message)}}
 async function signOut(){try{await api.logout()}catch{}clearSession();setMe(null);setActive(null);setMessages([])}
 if(!me)return <Auth onAuth={u=>{setMe(u);saveSession({...getSession(),user:u});api.presence().catch(()=>{});api.users().then(setPeople).catch(()=>{})}}/>;
 return <><div className="app"><aside className={`sidebar ${mobile?'mobile-open':''}`}><div className="brand"><div className="logo">Z</div><span>ZUNO</span><button className="close" onClick={()=>setMobile(false)}><X size={20}/></button></div><button className="profile" onClick={()=>setProfileOpen(true)}><Avatar user={me} showStatus/><div><b>{me.name}</b><small>{me.phone}</small></div></button><div className="tabs"><button className="active"><MessageCircle size={18}/> Chats</button><button><Users size={18}/> Groups</button></div><div className="search"><Search size={17}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search people"/></div><div className="chat-list">{filtered.map(p=><button className={`chat-row ${active?.id===p.id?'selected':''}`} key={p.id} onClick={()=>openChat(p)}><span className="profile-click" onClick={e=>{e.stopPropagation();setViewProfile(p)}}><Avatar user={p} showStatus/></span><div className="chat-info"><div><b>{p.name}</b>{Number(unread[`count:${p.id}`]||0)>0&&<span className="unread-badge">{Math.min(99,Number(unread[`count:${p.id}`]))}</span>}</div><p>{shownOnline(p)?'Online':'Offline'}</p></div></button>)}{filtered.length===0&&<div className="empty-chat">No other ZUNO users yet.</div>}</div><button className="new-chat" onClick={()=>filtered[0]&&openChat(filtered[0])}><Plus size={18}/> New conversation</button><div className="side-bottom"><button onClick={()=>setProfileOpen(true)}><Settings size={19}/> Profile & settings</button><button onClick={signOut}><LogOut size={19}/> Sign out</button></div></aside><main className="chat">{active?<><header className="chat-head"><button className="hamb" onClick={()=>setMobile(true)}><Menu/></button><button className="profile-click profile-head-click" onClick={()=>setViewProfile(active)}><Avatar user={active} showStatus/></button><div className="head-info"><b>{active.name}</b><span className={shownOnline(active)?'online':''}>{shownOnline(active)?'Online':'Offline'}</span></div></header><section className="messages"><div className="day">TODAY</div>{messages.map(m=><div key={m.id} className={`bubble-wrap ${m.sender_id===me.id?'mine':''}`}><div className="bubble">{m.body}</div></div>)}{messages.length===0&&<div className="empty-chat">No messages yet. Say hello 👋</div>}</section><div className="composer"><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Write a message..."/><button className="send" onClick={send}><Send size={18}/></button></div></>:<div className="empty-state"><div className="logo">Z</div><h2>Welcome to ZUNO</h2><p>Select someone to start chatting.</p></div>}{error&&<div className="error toast">{error}<button onClick={()=>setError('')}><X size={14}/></button></div>}</main></div>{profileOpen&&<ProfileEditor me={me} onClose={()=>setProfileOpen(false)} onSaved={u=>{setMe(u);saveSession({...getSession(),user:u});refreshPeople()}}/>}{viewProfile&&<ProfileViewer user={viewProfile} onClose={()=>setViewProfile(null)}/>}</>
}

ReactDOM.createRoot(document.getElementById('root')).render(<App/>);
