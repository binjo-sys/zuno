import React,{useMemo,useState} from 'react';
import {createRoot} from 'react-dom/client';
import {Search,Plus,MessageCircle,Users,Settings,Send,Paperclip,Smile,Phone,Video,MoreVertical,CheckCheck,Menu,X,LogOut} from 'lucide-react';
import './styles.css';
import {registerUser,loginUser,logout,currentUser,users,saveChats,chats as loadChats} from './lib/storage';

const demo=[{id:'demo-aisha',name:'Aisha',initials:'AK',status:'online',last:'Welcome to ZUNO 👋',time:'Now',messages:[['them','Hey! 👋'],['them','Welcome to ZUNO. Create an account to start chatting.']]},{id:'demo-brian',name:'Brian',initials:'BK',status:'last seen recently',last:'ZUNO is ready 🔥',time:'Now',messages:[['them','ZUNO is ready 🔥']]},{id:'demo-team',name:'ZUNO Team',initials:'ZT',status:'5 members',last:'Welcome to ZUNO 🎉',time:'Today',group:true,messages:[['them','Welcome to ZUNO 🎉']]}];
function initials(name='User'){return name.split(/\s+/).map(x=>x[0]).join('').slice(0,2).toUpperCase()}
function Auth({onAuth}){const [mode,setMode]=useState('login'),[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''); function submit(e){e.preventDefault();setError('');try{const u=mode==='login'?loginUser(email,password):registerUser({name,email,password});onAuth(u)}catch(err){setError(err.message)}}return <div className="auth"><div className="auth-card"><div className="logo auth-logo">Z</div><h1>Welcome to ZUNO</h1><p>Connect. Chat. Share.</p><div className="auth-tabs"><button className={mode==='login'?'on':''} onClick={()=>setMode('login')}>Sign in</button><button className={mode==='register'?'on':''} onClick={()=>setMode('register')}>Create account</button></div><form onSubmit={submit}>{mode==='register'&&<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Full name"/>}<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address"/><input required minLength="6" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password (6+ characters)"/>{error&&<div className="error">{error}</div>}<button className="primary" type="submit">{mode==='login'?'Sign in':'Create my ZUNO account'}</button></form><small>Your account is stored locally for this prototype. Cloud accounts come in the next backend stage.</small></div></div>}
function App(){const [me,setMe]=useState(currentUser());const [chatData,setChatData]=useState(loadChats());const [active,setActive]=useState('demo-aisha');const [text,setText]=useState('');const [search,setSearch]=useState('');const [mobile,setMobile]=useState(false);const [tab,setTab]=useState('chats');if(!me)return <Auth onAuth={setMe}/>;const stored=users().filter(u=>u.id!==me.id).map(u=>({id:u.id,name:u.name,initials:initials(u.name),status:'available',last:'Start a new conversation',time:'',messages:[]}));const chats=[...demo,...stored].map(c=>chatData[c.id]?{...c,...chatData[c.id]}:c);const current=chats.find(c=>c.id===active)||chats[0];const filtered=useMemo(()=>chats.filter(c=>c.name.toLowerCase().includes(search.toLowerCase())),[chats,search]);function send(){if(!text.trim())return;const value=text.trim();const next={...chatData,[current.id]:{last:value,time:'Now',messages:[...current.messages,['me',value]]}};setChatData(next);saveChats(next);setText('')}function newConversation(){const target=users().find(u=>u.id!==me.id);if(target)setActive(target.id)}return <div className="app"><aside className={`sidebar ${mobile?'mobile-open':''}`}><div className="brand"><div className="logo">Z</div><span>ZUNO</span><button className="close" onClick={()=>setMobile(false)}><X size={20}/></button></div><div className="profile"><div className="avatar me-avatar">{initials(me.name)}</div><div><b>{me.name}</b><small>{me.email}</small></div><button onClick={()=>{logout();setMe(null)}} title="Sign out"><LogOut size={18}/></button></div><div className="tabs"><button className={tab==='chats'?'active':''} onClick={()=>setTab('chats')}><MessageCircle size={18}/> Chats</button><button className={tab==='groups'?'active':''} onClick={()=>setTab('groups')}><Users size={18}/> Groups</button></div><div className="search"><Search size={17}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search conversations"/></div><div className="chat-list">{filtered.filter(c=>tab==='chats'||c.group).map(c=><button className={`chat-row ${active===c.id?'selected':''}`} key={c.id} onClick={()=>{setActive(c.id);setMobile(false)}}><div className="avatar">{c.initials}</div><div className="chat-info"><div><b>{c.name}</b><time>{c.time}</time></div><p>{c.last}</p></div></button>)}</div><button className="new-chat" onClick={newConversation}><Plus size={18}/> New conversation</button><div className="side-bottom"><button><Settings size={19}/> Settings</button></div></aside><main className="chat"><header className="chat-head"><button className="hamb" onClick={()=>setMobile(true)}><Menu/></button><div className="avatar">{current.initials}</div><div className="head-info"><b>{current.name}</b><span className={current.status==='online'?'online':''}>{current.status}</span></div><div className="head-actions"><button><Phone/></button><button><Video/></button><button><MoreVertical/></button></div></header><section className="messages"><div className="day">TODAY</div>{current.messages.map((m,i)=><div key={i} className={`bubble-wrap ${m[0]==='me'?'mine':''}`}><div className="bubble">{m[1]}{m[0]==='me'&&<span className="checks"><CheckCheck size={14}/></span>}</div></div>)}{current.messages.length===0&&<div className="empty-chat">No messages yet. Say hello 👋</div>}</section><div className="composer"><button><Paperclip/></button><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Write a message..."/><button><Smile/></button><button className="send" onClick={send}><Send size={18}/></button></div></main></div>}
createRoot(document.getElementById('root')).render(<App/>);
